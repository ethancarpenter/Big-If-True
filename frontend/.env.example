NEXT_PUBLIC_API_URL=http://localhost:5080

# Public by design - shown on the login page's demo section. Only set this
# once a production demo account actually exists (see DemoSeed:* on the
# backend); leave unset locally otherwise.
NEXT_PUBLIC_DEMO_EMAIL=demo@ethancarpenter.dev
